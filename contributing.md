# Contributing to VPP
- Use PRs to `main`; **PR review required**.
- Keep `vpp.json` version in sync with docs under `docs/spec/<version>/`.
- When bumping spec: duplicate `docs/spec/<version>/` to `docs/latest/` and update links in `vpp.json`.
- Examples are licensed under CC BY 4.0; code under MIT.
