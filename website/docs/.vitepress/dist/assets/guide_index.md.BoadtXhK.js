import{_ as t,c as o,o as a,ag as i}from"./chunks/framework.CIDzFKZP.js";const g=JSON.parse('{"title":"Guide","description":"","frontmatter":{"title":"Guide"},"headers":[],"relativePath":"guide/index.md","filePath":"guide/index.md","lastUpdated":1763224086000}'),s={name:"guide/index.md"};function r(n,e,d,l,c,p){return a(),o("div",null,[...e[0]||(e[0]=[i('<h2 id="tags-and-loci" tabindex="-1">Tags and loci <a class="header-anchor" href="#tags-and-loci" aria-label="Permalink to &quot;Tags and loci&quot;">​</a></h2><p>Every user turn starts with <code>!&lt;tag&gt;</code> on the first line. The assistant mirrors that tag (e.g., <code>&lt;g&gt;</code>). Tags map to loci defined in the header snippet and spec:</p><ul><li><code>&lt;g&gt;</code> — concept gathering; share context, snippets, or requirements without full deliverables.</li><li><code>&lt;q&gt;</code> — rough-context questioning and diagnostics; ask broad questions to establish direction.</li><li><code>&lt;o&gt;</code> — realized drafts that include assumptions, citations, and tests when relevant.</li><li><code>&lt;c&gt;</code> — fine-context critique or clarification; probe gaps and request adjustments.</li><li><code>&lt;o_f&gt;</code> — final delivery stage; any <code>&lt;o&gt;</code> turn can also be an <code>&lt;o_f&gt;</code> when the work is ready to ship.</li><li><code>&lt;e&gt;</code> / <code>&lt;e_o&gt;</code> — escape tags that redirect the conversation when you need to jump elsewhere in the loop.</li></ul><p>Modifiers or footer metadata can further label who owns a locus (for example, <code>Locus=assistant-analyst</code>).</p><h2 id="modifiers" tabindex="-1">Modifiers <a class="header-anchor" href="#modifiers" aria-label="Permalink to &quot;Modifiers&quot;">​</a></h2><p>Modifiers provide nuance on the same first line as the tag. VPP reserves:</p><ul><li><code>--correct</code> and <code>--incorrect</code></li><li><code>--minor</code> and <code>--major</code></li><li><code>--&lt;tag&gt;</code> (paired with <code>!&lt;o&gt; --correct</code> or <code>!&lt;e&gt;</code>)</li></ul><p>Additional structured flags (e.g., <code>--assumptions=3</code>) are allowed but should be documented in your project so downstream tooling can interpret them. Keep modifiers sparse to avoid conflicting instructions.</p><h2 id="pipelines-and-cycles" tabindex="-1">Pipelines and cycles <a class="header-anchor" href="#pipelines-and-cycles" aria-label="Permalink to &quot;Pipelines and cycles&quot;">​</a></h2><p>The loop is flexible—<code>&lt;g&gt; → &lt;q&gt; → &lt;o&gt; → &lt;c&gt; → … → &lt;o_f&gt;</code> in any order or length. After three cycles the assistant proposes an escape (<code>!&lt;e&gt; --&lt;tag&gt;</code> or <code>!&lt;e_o&gt;</code>) as outlined in the header snippet. Use the footer’s <code>Cycle=&lt;i&gt;/3</code> field to keep track of progress and resets.</p><h2 id="escapes" tabindex="-1">Escapes <a class="header-anchor" href="#escapes" aria-label="Permalink to &quot;Escapes&quot;">​</a></h2><p><code>&lt;e&gt;</code> is an escape tag paired with a modifier (<code>!&lt;e&gt; --&lt;g&gt;</code>, <code>!&lt;e&gt; --&lt;q&gt;</code>, etc.) to redirect the conversation to another locus. <code>&lt;e_o&gt;</code> is the dedicated form for immediate transitions into <code>&lt;o&gt;</code>. Use these escapes when the current loop needs to jump to a different phase without violating the grammar. Refer to the <a href="https://cdn.jsdelivr.net/gh/cbassuarez/viable-prompt-protocol@main/spec/latest/spec.md" target="_blank" rel="noreferrer">normative spec</a> for the full grammar and validation rules.</p><h2 id="implementation-notes" tabindex="-1">Implementation notes <a class="header-anchor" href="#implementation-notes" aria-label="Permalink to &quot;Implementation notes&quot;">​</a></h2><ul><li>Mirror the user’s tag at the top of every assistant response.</li><li>Validate modifier combinations before generating content.</li><li>Always end with the compliance footer; treat missing fields as bugs.</li><li>Store the header snippet in custom instructions or the system prompt so each conversation starts from the same contract.</li><li>Log each cycle alongside footer data to power analytics and audits.</li></ul>',14)])])}const u=t(s,[["render",r]]);export{g as __pageData,u as default};
