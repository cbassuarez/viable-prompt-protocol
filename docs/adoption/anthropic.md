# Adoption — Anthropic (Claude)

- Put Header Snippet in **system**. First user message uses `!<g>` line-1 grammar.
- Ensure client renders assistant footer line verbatim (post-processing off).
- For browsing/tooling, route via your app; VPP still requires citations when external facts are used.
