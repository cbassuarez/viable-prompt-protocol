# Adoption — Google Gemini

- Use Header Snippet in **system / developer** field. Enforce line-1 parsing upstream.
- If using function-calling, keep the footer untouched and log it alongside tool calls.
